import type { Product } from '../types'

/**
 * Static product catalog — intentionally EMPTY.
 *
 * All products were removed from the website, database, and Cloudinary.
 * The site now serves products exclusively from the backend API.
 * This file exists only to keep existing imports valid; it contains no data.
 */
export const products: Product[] = []
